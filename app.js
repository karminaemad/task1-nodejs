//Task-1
//(1)
const person = {
    fname : 'ahmed',
    lname : 'hossam',
    age : 20 ,
    city : 'alex',
}
console.log(person);
// /////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//(2)
const personJson = JSON.stringify(person);
console.log(personJson);
// ////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//(3)
const fs = require('fs');
fs.writeFileSync('data.Json',personJson);
// /////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//(4)
console.log(fs.readFileSync('data.Json').toString());
// /////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//(5)
const personObj = JSON.parse(personJson);
console.log(personObj);
///////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//(6)
let x = require('./update.js');
console.log(x);
///////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//(7)
const xJson= JSON.stringify(x);
console.log(xJson);
////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//(8)
fs.writeFileSync('udate.Json',xJson);
////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
